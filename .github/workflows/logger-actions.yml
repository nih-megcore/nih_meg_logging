name: nih2mne-tests
on: [push]
jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: apt-get install pip 
      - run: pip install pytest
      - run: pytest -vv
